# Optional. Copy this file to ".env" and add your key, OR set the variable in your shell.
#
# Without a key, everything runs OFFLINE with deterministic canned responses.
# With a key, the agents call Google Gemini for real.
#
# Get a key at https://aistudio.google.com/apikey
GEMINI_API_KEY=your-key-here
